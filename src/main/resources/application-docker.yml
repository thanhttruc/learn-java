spring:
  datasource:
    url: jdbc:mysql://mysql:3306/app_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: trucdang02


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

server:
  port: 8080