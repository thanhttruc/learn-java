spring:
  datasource:
    url: jdbc:mysql://mysql:3306/app_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: trucdang02

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

  minio:
    url: http://minio:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: myapp-avatars

server:
  port: 8080